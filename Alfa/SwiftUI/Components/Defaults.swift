//
//  Defaults.swift
//  Ostovaneh
//
//  Created by Sina khanjani on 8/5/1400 AP.
//

import SwiftUI

// MARK: - Typealias
typealias onTappedHandler = (() -> Void)

// MARK: - Padding or Constraint
/// This variable `DEFAULT_PADDING_00` is used to store a specific value in the application.
let DEFAULT_PADDING_00: CGFloat = 0.0
/// This variable `DEFAULT_PADDING_04` is used to store a specific value in the application.
let DEFAULT_PADDING_04: CGFloat = 4.0
/// This variable `DEFAULT_PADDING_08` is used to store a specific value in the application.
let DEFAULT_PADDING_08: CGFloat = 8.0
/// This variable `DEFAULT_PADDING_16` is used to store a specific value in the application.
let DEFAULT_PADDING_16: CGFloat = 16.0
/// This variable `DEFAULT_PADDING_32` is used to store a specific value in the application.
let DEFAULT_PADDING_32: CGFloat = 32.0

// MARK: - Ratio
/// This variable `DEFAULT_RATIO_58` is used to store a specific value in the application.
let DEFAULT_RATIO_58: CGFloat = 58.0 // Button height

// MARK: - Radius
/// This variable `DEFAULT_RADIUS_02` is used to store a specific value in the application.
let DEFAULT_RADIUS_02: CGFloat = 16.0
/// This variable `DEFAULT_RADIUS_04` is used to store a specific value in the application.
let DEFAULT_RADIUS_04: CGFloat = 4.0
/// This variable `DEFAULT_RADIUS_16` is used to store a specific value in the application.
let DEFAULT_RADIUS_16: CGFloat = 16.0

// MARK: - Font Size
/// This variable `DEFAULT_SIZE_04` is used to store a specific value in the application.
let DEFAULT_SIZE_04: CGFloat = 4.0
/// This variable `DEFAULT_SIZE_13` is used to store a specific value in the application.
let DEFAULT_SIZE_13: CGFloat = 13.0
/// This variable `DEFAULT_SIZE_16` is used to store a specific value in the application.
let DEFAULT_SIZE_16: CGFloat = 16.0
/// This variable `DEFAULT_SIZE_19` is used to store a specific value in the application.
let DEFAULT_SIZE_19: CGFloat = 19.0
/// This variable `DEFAULT_SIZE_21` is used to store a specific value in the application.
let DEFAULT_SIZE_21: CGFloat = 19.0
/// This variable `DEFAULT_SIZE_24` is used to store a specific value in the application.
let DEFAULT_SIZE_24: CGFloat = 24.0
/// This variable `DEFAULT_SIZE_28` is used to store a specific value in the application.
let DEFAULT_SIZE_28: CGFloat = 28.0
/// This variable `DEFAULT_SIZE_44` is used to store a specific value in the application.
let DEFAULT_SIZE_44: CGFloat = 44.0

// MARK: - Text
/// This variable `CANCEL_` is used to store a specific value in the application.
let CANCEL_ = "خیر"
/// This variable `YES_` is used to store a specific value in the application.
let YES_ = "بله"
/// This variable `OK_` is used to store a specific value in the application.
let OK_ = "باشه"
